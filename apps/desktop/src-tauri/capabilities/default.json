{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capability for the empty T009 desktop host.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
