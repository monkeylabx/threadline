// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: member.sql

package dbgen

import (
	"context"
)

const createMember = `-- name: CreateMember :one
INSERT INTO domain.members (
  tenant_id,
  actor_type,
  actor_id,
  display_name,
  role,
  state,
  org_unit_path
) VALUES (
  $1,
  $2,
  $3,
  $4,
  $5,
  $6,
  $7
)
RETURNING tenant_id, actor_type, actor_id, display_name, role, state, org_unit_path, joined_at
`

type CreateMemberParams struct {
	TenantID    string
	ActorType   int16
	ActorID     string
	DisplayName string
	Role        int16
	State       int16
	OrgUnitPath *string
}

func (q *Queries) CreateMember(ctx context.Context, arg CreateMemberParams) (DomainMember, error) {
	row := q.db.QueryRow(ctx, createMember,
		arg.TenantID,
		arg.ActorType,
		arg.ActorID,
		arg.DisplayName,
		arg.Role,
		arg.State,
		arg.OrgUnitPath,
	)
	var i DomainMember
	err := row.Scan(
		&i.TenantID,
		&i.ActorType,
		&i.ActorID,
		&i.DisplayName,
		&i.Role,
		&i.State,
		&i.OrgUnitPath,
		&i.JoinedAt,
	)
	return i, err
}

const getMember = `-- name: GetMember :one
SELECT tenant_id, actor_type, actor_id, display_name, role, state, org_unit_path, joined_at
FROM domain.members
WHERE tenant_id = $1
  AND actor_type = $2
  AND actor_id = $3
`

type GetMemberParams struct {
	TenantID  string
	ActorType int16
	ActorID   string
}

func (q *Queries) GetMember(ctx context.Context, arg GetMemberParams) (DomainMember, error) {
	row := q.db.QueryRow(ctx, getMember, arg.TenantID, arg.ActorType, arg.ActorID)
	var i DomainMember
	err := row.Scan(
		&i.TenantID,
		&i.ActorType,
		&i.ActorID,
		&i.DisplayName,
		&i.Role,
		&i.State,
		&i.OrgUnitPath,
		&i.JoinedAt,
	)
	return i, err
}

const updateMemberRoleState = `-- name: UpdateMemberRoleState :one
UPDATE domain.members
SET
  role = $1,
  state = $2
WHERE tenant_id = $3
  AND actor_type = $4
  AND actor_id = $5
RETURNING tenant_id, actor_type, actor_id, display_name, role, state, org_unit_path, joined_at
`

type UpdateMemberRoleStateParams struct {
	Role      int16
	State     int16
	TenantID  string
	ActorType int16
	ActorID   string
}

func (q *Queries) UpdateMemberRoleState(ctx context.Context, arg UpdateMemberRoleStateParams) (DomainMember, error) {
	row := q.db.QueryRow(ctx, updateMemberRoleState,
		arg.Role,
		arg.State,
		arg.TenantID,
		arg.ActorType,
		arg.ActorID,
	)
	var i DomainMember
	err := row.Scan(
		&i.TenantID,
		&i.ActorType,
		&i.ActorID,
		&i.DisplayName,
		&i.Role,
		&i.State,
		&i.OrgUnitPath,
		&i.JoinedAt,
	)
	return i, err
}
