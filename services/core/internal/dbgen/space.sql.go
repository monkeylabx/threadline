// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: space.sql

package dbgen

import (
	"context"
)

const createSpace = `-- name: CreateSpace :one
INSERT INTO domain.spaces (
  tenant_id,
  space_id,
  display_name,
  discoverable
) VALUES (
  $1,
  $2,
  $3,
  $4
)
RETURNING tenant_id, space_id, display_name, discoverable, created_at
`

type CreateSpaceParams struct {
	TenantID     string
	SpaceID      string
	DisplayName  string
	Discoverable bool
}

func (q *Queries) CreateSpace(ctx context.Context, arg CreateSpaceParams) (DomainSpace, error) {
	row := q.db.QueryRow(ctx, createSpace,
		arg.TenantID,
		arg.SpaceID,
		arg.DisplayName,
		arg.Discoverable,
	)
	var i DomainSpace
	err := row.Scan(
		&i.TenantID,
		&i.SpaceID,
		&i.DisplayName,
		&i.Discoverable,
		&i.CreatedAt,
	)
	return i, err
}

const getSpace = `-- name: GetSpace :one
SELECT tenant_id, space_id, display_name, discoverable, created_at
FROM domain.spaces
WHERE tenant_id = $1
  AND space_id = $2
`

type GetSpaceParams struct {
	TenantID string
	SpaceID  string
}

func (q *Queries) GetSpace(ctx context.Context, arg GetSpaceParams) (DomainSpace, error) {
	row := q.db.QueryRow(ctx, getSpace, arg.TenantID, arg.SpaceID)
	var i DomainSpace
	err := row.Scan(
		&i.TenantID,
		&i.SpaceID,
		&i.DisplayName,
		&i.Discoverable,
		&i.CreatedAt,
	)
	return i, err
}

const updateSpaceDirectoryMetadata = `-- name: UpdateSpaceDirectoryMetadata :one
UPDATE domain.spaces
SET
  display_name = $1,
  discoverable = $2
WHERE tenant_id = $3
  AND space_id = $4
RETURNING tenant_id, space_id, display_name, discoverable, created_at
`

type UpdateSpaceDirectoryMetadataParams struct {
	DisplayName  string
	Discoverable bool
	TenantID     string
	SpaceID      string
}

func (q *Queries) UpdateSpaceDirectoryMetadata(ctx context.Context, arg UpdateSpaceDirectoryMetadataParams) (DomainSpace, error) {
	row := q.db.QueryRow(ctx, updateSpaceDirectoryMetadata,
		arg.DisplayName,
		arg.Discoverable,
		arg.TenantID,
		arg.SpaceID,
	)
	var i DomainSpace
	err := row.Scan(
		&i.TenantID,
		&i.SpaceID,
		&i.DisplayName,
		&i.Discoverable,
		&i.CreatedAt,
	)
	return i, err
}
